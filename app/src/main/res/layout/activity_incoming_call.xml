<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingBottom="@dimen/activity_vertical_margin"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin"
    android:id="@+id/incoming_call_window"
    tools:context=".IncomingCallActivity">

    <LinearLayout
        android:id="@+id/buttons"
        android:layout_width="match_parent"
        android:layout_height="wrap_content">
        <Button
            android:id="@+id/declineButton"
            android:layout_weight=".48"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:text="Decline"
            android:drawableRight="@drawable/ic_call_end_white_48dp"
            android:onClick="declineCall"
            android:background="@drawable/button_red"
            android:textColor="#ffffff"
            android:textSize="24dp"
            android:layout_margin="10dp"/>
        <Button
            android:id="@+id/acceptButton"
            android:layout_weight=".48"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_alignParentTop="true"
            android:layout_toRightOf="@id/declineButton"
            android:drawableRight="@drawable/ic_call_white_48dp"
            android:text="Accept"
            android:onClick="acceptCall"
            android:background="@drawable/button_green"
            android:textColor="#ffffff"
            android:textSize="24dp"
            android:layout_margin="10dp"/>
    </LinearLayout>
</RelativeLayout>
